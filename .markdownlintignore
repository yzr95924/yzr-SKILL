# markdownlint 忽略清单——这些文件不是"待 lint 的作者散文"，强行 lint 只生噪音：
#
# canonical/  : 字节级金标准（探测器 check_wiki_fixtures.py 逐字比对 deployed wiki 文件
#               与这些 .md）。改一个字符就破坏 detector，必须原样保留——lint 结果无意义。
# fixtures/*.txt : 同上，CLI 落盘骨架的字节金标准（.gitignore / index.md / log.md ...）。
#                  README.md 是作者文档，仍参与 lint（不在本忽略范围）。
**/references/canonical/**
**/references/fixtures/*.txt
