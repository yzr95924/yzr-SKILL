# Wiki Spec 版本历史（Changelog）

> 本文件原为 `wiki-spec.md` 附录 B（spec 演进日志）。**CLI 实现不读本文件**（落盘按 spec
> §1-§14 当前规则走）；**SKILL 内容只在迁移时由 agent 人读**（migrate-workflow.md 定位为
> 迁移依据 SSOT）——其余时刻按需 Read 追"为什么这条规则存在" / "哪个版本引入"。
> 表格 cell 内连带的 `SKILL.md` / `lint-checklist.md` / `agents-md-template.md` 同步文件清单是
> spec 演进时实际改过的范围——回看"那个版本的提交动了哪些文件"用。

## 附录 B：版本历史

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 0.32.0 | 2026-08-16 | **AGENTS.md 模板审计修复 + §九 改名「yzr 个人工作习惯」（语言风格降为子节）**——**根因**：对 `agents-md-template.md` 全量 review 发现 5 类问题——① MEMORY 段 SSOT 反指指向仓库根 `MEMORY/MEMORY.md`「规则」段，但条目形式（完整 vs 短）的真正权威定义在 `wiki-spec.md` §5.2，且"（与项目根 `CLAUDE.md` 同步）"过时（CLAUDE.md 已是薄壳、两边格式刻意不同：仓库根三件套 vs wiki 仅 title 必填）；② 两处"详见顶部「L2 索引 `@import` 收口」段"引用不存在的段（顶部实际是「关键」段）；③ 三处悬空节号引用——"（§二 13）" ×2（应为 `lint-checklist.md` §二.13）+"在 §0 已排好"（应为 `wiki-spec.md` §6）；④ 8 处 SSOT 反指块逐字重复同一套"本文件是 wiki 仓自带模板…必须自包含"理由（L1 常驻上下文纯成本）；⑤ §七 与顶部 blockquote 重复"全量重渲染不保留"；⑥ §二 节名"页面类型与 frontmatter 约定"名不副实（实际还含 Page Thresholds / 矛盾处理 Update Policy / Index 扩容三条操作纪律）。**机制**：① MEMORY 段反指改指 `wiki-spec.md` §5.2 + 删 CLAUDE.md 同步声明；② 两处悬空引用改指顶部「关键」段；③ 三处悬空节号引用补全文档名；④ 顶部 blockquote 新增「`SSOT 反指` 块约定」段（自包含理由 + 同步义务一次性定义，随拷贝走，wiki 侧仍自足），8 处反指块各缩为一行 `> **SSOT 反指**：本段权威定义在 <位置>，改 SSOT 时同步改本段。`（省 ~14 行 L1 常驻）；⑤ §七 去重句 + 加"详见顶部说明"指针；⑥ §二 改节名「页面类型、frontmatter 与操作阈值」（小节不挪位、节号不变）。**§九 改名**：`## 九、交互语言风格` → `## 九、yzr 个人工作习惯`（个人习惯定位——agent 按 yzr 的习惯工作而非通用规范），原两条语言风格规则逐字保留、下移为 `### 语言风格（对用户不用黑话，严谨精确）` 子节；**新增两个子节**：`### 遇阻退后一步`（不一根筋：同一思路连续两次未果 / 修改面越滚越大 / 想绕过纪律 任一信号即退后，三步——回头看路径倒查 / 第一性原理重审目标与约束 / 举一反三按"类"报同类问题）+ `### 表达先归纳后落笔`（不把输入清单逐项复述成结构：先合并同义 + 每句过删除测试；由来 = 本轮首版措辞冗余的 meta 教训——用户关键词当结构锚点复述 / 逐项复述未先归纳 / 加法当安全默认）。正反对照例子**非格式义务**——只语言风格保留（抽象口径需语气校准），两条新习惯本身是可执行动作清单、例子属于把规则换说法复述，删（由「每句过删除测试」当场自证）。SKILL.md 核心原则 §13 标题同步改「yzr 个人工作习惯——当前含：语言风格 / 遇阻退后一步 / 表达先归纳后落笔」+ 正文镜像 + 末行指针同步。**破坏性**：模板正文 §一/§二/§七/§九 字节全变——老 wiki 升级后 `agents-md-template-sync` 报 error，按既有 resync 路径全量重渲染一次即对齐（本地定制先沉淀 `MEMORY/`）；无新迁移 action / legacy pattern。SKILL.md `metadata.wiki_spec_version` 升至 0.32.0 + `last_modified` 更新（`fixtures_check_count` 不变仍 20——无新增 fixtures 检查）；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.32.0 |
| 0.31.0 | 2026-08-16 | **机械操作脚本化——「脚本只探测不改 wiki」原则精化（准入规则原文）+ 新 writer 脚本 `wiki_write.py`**——**根因**：大量 md 纪律在约束"零判断的字节操作"（log 格式 / index 条目格式 / `updated` 改今天 / 清 reviewed 戳 / MEMORY 索引行），"md 写规则 → agent 手工 Edit/Write → lint 兜底"三层成本，每层都在说话（正文多且杂的主因）。**原则精化**（replace 0.27.0 起的"脚本只探测不改 wiki"表述；skill-creator 审计标准「机械操作脚本化」同步收录）：**一个写操作进脚本，当且仅当 (1) 输出字节是输入的纯函数（不读正文、无权衡、无用户偏好）；(2) lint 已有对应检查能验证产物（round-trip 可测）**。两条缺一 → 留 agent/md；**迁移期一律 agent**（迁移 = 格式未定之时，脚本只认识当前形态）。逃生舱：手写始终合法、lint 守门——脚本是默认路径不是闸门；脚本不自带内容知识（不生成摘要 / 不嵌正文模板 / 不做语义合并）。**新脚本 `scripts/wiki_write.py`**（五子命令，py3.7 + 零依赖，frontmatter 正则改写不引 PyYAML）：`log`（追加严格格式条目，`--op` + 可重复 `--title` 或 `--bulk --topic --count`，写完自动截断保最近 `LOG_RETENTION_LIMIT` 条、frontmatter 不动）；`index add\|remove`（从页 frontmatter 派生 title/description/type，类别段字母序插入 / 空段占位行清除 / 拒重复 / archive 用 remove）；`touch`（`updated`=现在 + 删 `reviewed`/`reviewed_at`）；`new`（5 必填 frontmatter + H1 脚手架，slug kebab 校验 + 拒覆盖，`--type source` 强制 `--sources`——lint §二.3 sources 非空是 error）；`memory add`（title-only frontmatter + 原子追加 MEMORY.md 索引行）。**格式常量单一来源**：wiki_write.py 从 lint_wiki.py / log_format.py import（VALID_TYPES / LOG_RETENTION_LIMIT / LOG_LINE_RE / parse_spec_version / SOURCE_NAME_RE）——spec 改格式时变更点不增。**版本错位防护**：writer 启动时读 wiki §八 与 `CURRENT_WIKI_SPEC` 不一致 → stderr 警告"先 migrate 再写"（只警告不阻断）。**md 收缩**：SKILL.md 新增 §设计决策「机械 vs 判断」节（准入规则原文 + 已固化 / 留 md 清单 + 逃生舱 + 反侵蚀信号）；核心原则 §4/§5/§9/§10 收成"跑 X 命令"单句；工作流 §1/§4 步骤坍缩；§5 Migrate 职责切分加"迁移期不走 wiki_write"；ingest-workflow.md Step 4-6 + 批处理命令化（机械步骤换命令，判断内容保留）；query-workflow.md 归档三步命令化；lint-checklist.md §二 机制细节（`_git_porcelain_path` / `Path(s).parts[2]` / `sp.exists()` 等）删净、只留 finding/严重性/触发/修法四列口径（478→302 行）；page-templates.md §7 / wiki-spec.md §4.1（截断职责改"写入时脚本自动 + lint 兜底"）/ agents-md-template.md log 段 / examples.md 样例二/三同步。**测试**：新增 `test_wiki_write.py`（25 用例，round-trip 不变量——new 产物过 `check_frontmatter`、log 产物过 `LOG_LINE_RE` + 截断保 frontmatter、memory 产物过 `check_memory_index`、index 条目派生自 frontmatter、版本告警）；全套 pytest 26→51。**contract 变更**：spec 字段 / 格式 / canonical 字节金标准**零变更**（writer 是正路增强，不是格式变更）；`fixtures_check_count` 仍 20。**破坏性**：① `agents-md-template.md` log 段措辞引 `wiki_write.py log` → 模板字节变，老 wiki 升级后 `agents-md-template-sync` 报一次 drift，按 resync 全量重渲染即对齐（本地定制照常逐条裁定）；② lint-checklist §二 缩编 → 迁移时读新清单。SKILL.md `metadata.wiki_spec_version` 升至 0.31.0；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.31.0 |
| 0.30.0 | 2026-08-14 | **交互语言风格纪律（SKILL.md 核心原则 §13 + `agents-md-template.md` §九）——对用户不用黑话、严谨精确**——**根因**：agent 与用户对话时把 skill 内部术语（ingest / lint / orient ritual / SSOT 等）原样抛给用户、用"处理了 / 相关的东西"这类含糊汇报，用户读不懂也核不了账——skill 是给 LLM 读的，但服务对象是人。**机制**：SKILL.md 核心原则新增 §13（SSOT）：面向用户时内部术语翻译成日常语言（"把资料整理进 wiki" / "检查 wiki 的健康状况"），必须提术语时括号给白话解释，禁网络行话 / 口头禅；严谨精确四条——动词说清结果（已创建 / 已更新 / 未执行 + 原因）、地点给相对路径、数量给具体数字、区分事实与推断（wiki 记载 vs 我推测，没依据不写进正文）；报告按三段（做了什么 / 没做什么及原因 / 需要你决定什么）。`agents-md-template.md` 新增 §九「交互语言风格」（自包含——wiki 仓自带模板跨仓引不到 SKILL.md，措辞与 SSOT 故意一致）；`scripts/check_wiki_fixtures.py` `check_agents_md_template_sync` docstring「正文 §一~§七」→「§一~§七 + §九」。**非破坏性（内容层）**：语言风格是软纪律，不改任何文件格式 / 字段 / 脚本契约；合规 0.29.0 wiki 升级 = 按既有 resync 路径全量重渲染 AGENTS.md（`fixtures-fix-agents-md-resync` 4 步，§八 四变量自动保留，新增 §九 自动落盘）+ bump AGENTS.md §八版本号。SKILL.md `metadata.wiki_spec_version` 升至 0.30.0 + `last_modified` 更新；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.30.0。**破坏性**：老 wiki 升级后 `agents-md-template-sync` 报 error（AGENTS.md 缺 §九 段）——按 resync 全量重渲染一次即对齐；本地定制纪律需先沉淀 `MEMORY/` 再重渲染（既有 0.26.0 机制，无新增迁移步骤） |
| 0.29.0 | 2026-07-31 | **raw/ 写权限两处定向放开：`raw/discussions/` 协作草稿层（新）+ `raw/external/` target 角色切分（澄清）**——**根因**：raw/ 总纪律（LLM 只读）在实际使用中卡了两类真需求：① 用户希望 LLM 直接编辑仓内的临时讨论 / 设计草稿 markdown，而不是只能打印出来让用户手抄；② 用户希望对 `raw/external/` 接入的**自有**代码仓做开发协作（修 bug / 重构），不想换目录开会话。原纪律一刀切"raw/ 全只读"把这两类合理诉求也禁了。**机制**：分两处定向放开，**不**稀释 raw/ 单一语义。**(A) `raw/discussions/`——新增协作草稿子树（spec §15）**：用户 + LLM **双方可写**（raw/ 内除 §13.3 外唯一 LLM 可写子树）；按需创建（CLI 不预建，与 `raw/external/` 同型先例）；不要求 frontmatter；**不**进 `wiki/index.md`；写 discussions/ **不**追加 log（非 wiki 操作，显式豁免"每次写入必更 log"）；git 默认跟踪。可写性靠三道脚本契约兜底（不靠自觉）：`ingest_diff.py` 的 `collect_raw_files()` `skip_dirs` 加 `raw/discussions/`（草稿不当 untracked 素材，堵 provenance 后门）+ `lint_wiki.py` `check_raw_immutable()` 从 `git status` 改动里排除 `raw/discussions/` 路径（草稿改动不触发 raw-modified 告警）+ 新 finding `source-in-discussions`（error，`type: source` 页 `sources:` 不得指向 discussions/，堵"草稿直接被引用为真相源"）。草稿消化进 wiki 两条路（都需用户确认）：消化式（结论写 wiki 页）或转正式（`mv` 到 `raw/articles/` 等子树后走标准 ingest——raw/ 只读的第二处 mv 例外）。**(B) `raw/external/` target 角色切分（spec §13.3 澄清）**：把"**不**修改 target 本身"修正为按角色分——**wiki 维护操作**（ingest / query / lint / migrate）中 target 仓内文件只读（librarian 角色）；**用户明确要求的开发协作**不属 wiki 操作、不受 raw/ 只读约束（target 在 wiki 仓外、有其自身 git、由用户全权处置）；代码改动后的 wiki 同步走**既有通道**（`external-git-anchor-stale` → 刷新 anchor + 受影响 source 页重 ingest），**不**加新机制。**为什么是 raw/ 内子树而不是新顶层目录**（A）：草稿与 raw/ 资料同属"非 wiki 复利资产"的原始层，放 raw/ 下让"raw/ = 原始层"心智模型保持单一，再用显式纪律 + 脚本豁免守住"discussions/ 不是真相源"。**滑坡防线**（§15.4）：discussions/ 是第二处例外，纪律型 skill 怕"同层双语义"被外扩——显式禁止把可写性外推到 papers/ articles/ 等其他子树、禁止把 §13.3 target 切分外推为"raw/ 也能改"、禁止用 discussions/ 规避 provenance（绕 §15.3 漏 log / index / reviewed 戳）。**同步改的 spec 段**：`wiki-spec.md` §1 目录树（raw 子目录列表加 discussions/ + 两处例外点名）+ §13.3（target 角色切分重写）+ 新增 §15（§15.1 动机边界 / §15.2 脚本契约 / §15.3 归档路径 / §15.4 滑坡防线）+ 目录；`SKILL.md` §四层架构第 1 层 + §核心原则 §1 + §边界 + §反模式（滑坡防线 + 角色切分）；`agents-md-template.md` §一（external 段角色切分 + 新 discussions/ 子段，跨仓引不到 SKILL.md 必须自包含）；`lint-checklist.md` §二.1（raw-modified 排除 discussions/）+ §二.3（新 source-in-discussions finding 文档）；`page-templates.md` sources 字段加禁止指向 discussions/ 注记。**脚本**：`scripts/ingest_diff.py` `skip_dirs` 加 `raw/discussions/` + docstring；`scripts/lint_wiki.py` 新常量 `DISCUSSIONS_SUBDIR` + 新 `_git_porcelain_path()` helper + `check_raw_immutable()` 过滤 discussions/ + `check_frontmatter()` 加 `source-in-discussions` 分支 + `severity_of()` 加 `source-in-discussions → error`。`SKILL.md` `metadata.wiki_spec_version` 升至 0.29.0；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.29.0；`metadata.fixtures_check_count` **不变仍 20**（discussions/ 按需创建、fresh init 字节不变，`check_wiki_fixtures.py` 不增 check、fixtures/canonical 不重生成）。**破坏性**：fresh init 字节不变（discussions/ 按需）；`source-in-discussions` 对存量 wiki 零影响（无 sources 指向不存在的 discussions/，不报）；存量 wiki 升级后唯一新发现是 `agents-md-template-sync` 报 AGENTS.md §一 与新模板（external 角色切分 + 新 discussions/ 子段）字节不一致——走既有 `fixtures-fix-agents-md-resync` action（0.26.0 机制）全量重渲染 + 本地定制搬 `MEMORY/` 即对齐，**无需新迁移 action / legacy pattern**。**外部 CLI 需同窗口对齐**（`llmw/__init__.py` `WIKI_SPEC_VERSION` + CLI submodule 指针）——按 spec 版本号 bump 三方原子操作约定；本仓 `lint_wiki.py --check-version` 在该对齐完成前对老 CLI 落盘的 wiki 报 `wiki-spec-version-stale` warn（属预期） |
| 0.28.0 | 2026-07-29 | **frontmatter `created` / `updated` + log 条目加 `HH:MM` 粒度（backward-compat 解析）**——**根因**：同日多次 ingest / query / lint 越来越常见（批量摄取 + 增量 lint 节奏），日期粒度 `YYYY-MM-DD` 丢失同日内顺序信息；用户面对 `created: 2026-07-27` 无法判断相对新旧。**格式切换**：`created` / `updated` 字段由 `YYYY-MM-DD` → `YYYY-MM-DD HH:MM`（24h 无秒无时区，ISO 8601 子集，Python `datetime.strptime` 原生支持）；log.md 条目 `## [YYYY-MM-DD HH:MM] <op> | <title>` 同理。**backward-compat 策略**（核心）：scripts 层 `parse_date_or_datetime()`（`scripts/log_format.py`）按精度从低到高试三种格式——`%Y-%m-%d` / `%Y-%m-%d %H:%M` / `%Y-%m-%d %H:%M:%S`——老 wikis 的 `YYYY-MM-DD` 不报错、stale-summary 判定（`check_stale_summaries`）与 `--check-stale`（`raw_newer_than_source`）继续可用；`LOG_LINE_RE` / `LOG_INGEST_RE` 正则 `HH:MM[:SS]` 段可选——`HH:MM` 不强制即可匹配。`scripts/check_wiki_fixtures.py` 的 hardcode `LOG_LINE_RE` 同步更新（vendored 副本独立运行约束保留）。**保留 YYYY-MM-DD 不变**的字段（用户未点 / 各自 daily 粒度足够）：`published`（源材料发表日）/ `reviewed_at`（人工审核日）/ `captured_at`（anchor TOML 接入日）/ `✓ reviewed <date>`（index.md 行内人工徽章）。**同步改的 spec 段**：`wiki-spec.md` §1.2 `{{SETUP_DATE}}` + §3 + §4 字段表 + §4 log 正则 + §9 通用必填表；`page-templates.md` §一 字段说明 + 5 个 type 模板 + §7 log 示例；`agents-md-template.md` 顶部页模板 + log 格式严格段；`ingest-workflow.md` / `query-workflow.md` log 条目说明；`fixtures/README.md` `{{SETUP_DATE}}` 映射；`examples.md` 两个样例 log 条目时间；`canonical/{index,log}.md` 示范新格式 `2026-06-28 14:30`（fixtures 字节合规金标准）。`SKILL.md` `metadata.wiki_spec_version` 升至 0.28.0；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.28.0。**`test_lint_wiki.py` + `test_check_wiki_fixtures.py` 端到端通过**（3 + 11 用例）；`parse_date_or_datetime` 8 case smoke test 覆盖 `None` / 非 str / 格式错 / `2026-13-99` / 三种合法格式。**纪律推论**：版本演进信息仅在 `wiki-spec-changelog.md` 收口——正文 / 模板 / scripts docstring 不写"X.Y.Z+ 新格式"内联标注（漂移温床，已实验证：本次初版散布 13+ 处后用户当场要求清除）。**外部 CLI 需同窗口对齐**（`llmw/__init__.py` `WIKI_SPEC_VERSION` + CLI submodule 指针）——按 spec 版本号 bump 三方原子操作约定，本仓 `lint_wiki.py --check-version` 会在该对齐完成前对老 CLI 落盘的 wiki 报 `wiki-spec-version-stale` warn（属预期）。**非破坏性**：所有 0.27.1- 老 wiki 升级后 `lint_wiki.py` 行为不变——`parse_date_or_datetime` 宽容解析老 `YYYY-MM-DD`；`log_format` 正则 `HH:MM` 可选；旧页面静默合法。无强制迁移；用户在 ingest / query / lint 自然编辑时渐进切到新格式（`updated` 写一次 HH:MM 即对齐）。**本版本故意不注册 `legacy-date-format` legacy pattern**——单用户 skill + parser backward-compat + 迁移成本低（一行 Edit），加 lint finding 反而稀释日常 lint 注意力（详见 `~/.claude/projects/.../skill-docs-strip-inline-version-markers.md` + 当时对话上下文） |
| 0.27.1 | 2026-07-20 | **spec 职责收缩 + 探测器升格「可执行真源」+ 探测器测试补齐（CLI 单向依赖 SKILL 治理）**——`wiki_metadata.toml` 完整字段 schema 不再由本 spec 权威定义，改由 **CLI 代码 SSOT**（`llmw/wiki/store.py`）；spec 只描述 SKILL 读取所需的字段子集。前言软化「CLI 实现契约」+ 追加「探测器是可执行真源」声明（spec 与探测器不一致时以探测器为准）；§1 目录树补 `wiki_metadata.toml` 行（出生骨架完整性）；新增 §1.1 `wiki_metadata.toml`（schema 边界 + skill 读取契约：6 字段 `name` / `topic` / `display_name` / `description` / `tags` / `created_at`；`model` / `schema_version` / `updated_at` 归 CLI，skill 不读）；§12（toml schema 归 CLI）同步。**探测器升格**：`check_wiki_fixtures.py` 明确定为 CLI 产物合规的可执行真源；新增 `wiki-metadata-reads-satisfied` check（读取契约自洽——校验 SKILL scan 读取的 6 字段存在；显式函数模式非 SKELETON_SPECS；文件缺失 → fail）→ **19→20 条**（`metadata.fixtures_check_count` 19→20）。**新建 `test_check_wiki_fixtures.py`**（11 用例 unittest + subprocess 端到端，standalone 不依赖 CLI；对称 workspace 侧测试）——探测器升格真源后自身质量成单点，必须有测试兜底。SKILL.md 探测器描述升格「可执行真源」+ `metadata.wiki_spec_version` 升至 0.27.1；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.27.1。**动机**：toml schema 与 CLI 实现强绑定，spec 无法同步；探测器原完全不读 `wiki_metadata.toml`，本 check 让读取契约首次可执行。**非破坏性**：fresh init 字节不变（CLI `wiki add` 本就落盘 9 字段 ⊇ SKILL 读 6）；探测器全 pass；极长尾的远古 wiki 若缺 6 字段之一会被首次 flag——跑探测器按报缺项补即可（标准 migrate，非阻断） |
| 0.27.0 | 2026-07-20 | **log.md 改滑动窗口（废 rotate 归档）——完整历史交给 git**——**根因**：log.md 同时背"审计时间线（要全）"+"近期活动速览（要短）"两个职责，原 rotate 机制（满 500 整篇归档 `log-YYYY.md`）调和不足——单文件仍涨到 500 才触发；且 `ingest_diff` 只读 `log.md` 不读归档，rotate 一次老 ingest 记录即出视野。新事实：本 skill 假设 wiki 纳入 workspace 级 git（完整操作历史在 `git log -p -- wiki/log.md`），log.md 无需再当完整审计，只做"最近 N 条活动速览 + 操作语义（ingest/query/lint）记录"。**机制切换**：`LOG_ROTATION_THRESHOLD=500` → `LOG_RETENTION_LIMIT=50`；`check_log_rotation`（报 `log-rotation-recommended` + 指向 rotate）→ `check_log_truncation`（报 `log-truncation-recommended` + 指向截断）；§4.1 Log rotation 整段删除换 §4.1 Log retention（滚动窗口 + 两层截断：写入时 agent 自查 + lint 兜底，删最旧保最近 N、frontmatter 不动）。**截断由 agent 用 Edit/Write 做**（脚本只探测不改 wiki——既有职责原则），非新增脚本；`ingest_diff` 核心功能（找未摄取文件靠 source 页 frontmatter）不受影响——log 只做次要孤儿交叉核对。**同步改**：migrate §6.5「严格保留」→「迁移期不改（不合并 / 不截断）」（接受迁移期 log.md 可能 >50）；page-templates §7 lint 项「不允许删除历史条目」→ 滚动窗口；agents-md-template log 段「仅追加操作时间线 / 不删不改只 append」→ 近期活动速览（滚动窗口）；SKILL.md 反模式表「log 是审计+时间线」→「操作语义+近期速览，完整历史靠 git」；ingest/query workflow 写 log 步骤加截断指针；wiki-spec §2 + fixtures/README.md「只 append」措辞同步。SKILL.md `metadata.wiki_spec_version` 升至 0.27.0（`fixtures_check_count` 不变仍 19——删的是 `lint_wiki.py` 的 check，非 `check_wiki_fixtures.py` 的 19 条）；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.27.0 + 常量/check/finding/severity 全套改名。**破坏性**：老 wiki（log.md > 50 条）升级后 lint 报 `log-truncation-recommended`（warning），agent 截一次保最近 50 即对齐（老条目在 git history 可回溯）；存量 `log-YYYY.md`（0.27.0 前 rotate 产生，实际罕见——阈值 500 极少触发）视为只读遗留，lint 不查、agent 不动。**无 git 的 wiki 老条目会丢失**——本 skill 推荐将 wiki 纳入 git 仓 |
| 0.26.0 | 2026-07-19 | **AGENTS.md 模板渲染字节比对（`agents-md-template-sync`）取代两条存在性检查——升级 = 全量重渲染 + 本地定制搬 `MEMORY/`**——**根因**：AGENTS.md 的 fixtures 检查长期只有"存在性断言"（0.24.0 的 `agents-md-has-at-imports` / 0.25.0 的 `agents-md-top-read-directive`），只增不删；migrate step 6 又明文"其它字段不动"——删旧段的依据只散落在 changelog「破坏性」注记里靠 agent 自觉逐版本执行，step 7 验证兜不住，结果版本号 bump 了、0.22.0 时代的正文还留着（实测两个真实 wiki：版本行 0.25.0/0.19.0，正文残留 65-113 行旧文本）。**机制切换的依据**：AGENTS.md per-wiki 变量只有 4 个（主题 / 创建日期 / CLI 版本 / Wiki Spec 版本——全在 H1 + §八），正文 §一~§七 跨 wiki 逐字相同，故可整文渲染比对。**新增** `agents-md-template-sync`（error）：从 wiki §八 提取三变量 + wiki 自钉版本（主题 fallback H1），渲染 `references/agents-md-template.md` 后字节比对——旧版本残留 / 本地改动一网打尽；**删除** `agents-md-has-at-imports` + `agents-md-top-read-directive` 两条 SKELETON_SPECS 及其专用信号处理器（`at_imports_present` / `top_read_directive` / 0.23.0 遗留 `no_at_imports`）——字节比对全覆盖。fixtures count 20 → **19**（删 2 增 1；顺带校正：0.24.0/0.25.0 changelog 行记的 "18" 与实际 code/metadata 的 20 不符，本次以 `CHECK_FUNCTIONS` 实注册数为准）。**修复路径**（plan 新 action `fixtures-fix-agents-md-resync`，4 步）：提取旧 §八 变量 → 渲染模板（`{{WIKI_SPEC_VERSION}}` 用 to_version）→ diff 旧文件，多出的定制行/段逐条与用户裁定搬 `MEMORY/`（短条目写索引行 / 含 why 建 `<slug>.md`）或丢弃 → Write 覆盖。**纪律推论**：wiki 特有纪律一律沉淀到 `MEMORY/`（AGENTS.md 顶部 `@import` 加载，会话常驻），不写进 AGENTS.md——否则下次重渲染丢失；模板头部 blockquote + §七 已加此锚点。SKILL.md `metadata.wiki_spec_version` 升至 0.26.0 + `fixtures_check_count` 20→19；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.26.0 + `build_migration_plan` 新增 resync 分支 + `agent_rules` 补一条；`scripts/check_wiki_fixtures.py` 新 check + 删两条 + docstring 计数校正；`references/wiki-spec.md` 新增 §10.1 + §14.7-§14.9 三节随重渲染机制落地移除（迁移指南由 `fixtures-fix-agents-md-resync` action 取代——本行初版误记为「§14.8/§14.9 标已取代、保留作历史记录」，实际三节均删除，2026-07-20 勘误）；`references/lint-checklist.md` §二.14 同步；`references/migrate-workflow.md` step 6 重写 + 「fixtures 字段更新清单」新增"模板渲染件"类别；`references/agents-md-template.md` 头部 + §七 加"定制进 MEMORY"锚点。**破坏性**：所有 AGENTS.md 与当前模板有字节差异的 wiki 升级后 `agents-md-template-sync` 报 error——按 resync 4 步走一次即永久对齐；此后任何 spec 升级 AGENTS.md 都只需重渲染，changelog 不再需"删 X 段"手工步骤 |
| 0.25.0 | 2026-07-18 | **对齐 `yzr-multi-agent-context` R1+R2：顶部强制 Read 指令收口 + 去品牌**——`AGENTS.md` 顶部新增**强制 Read 指令 blockquote**（"凡 `@path/to/file` 形式的引用…都用 Read 工具按需读取…不自动展开 `@import` 的 agent 尤须手动执行"，逐字移植自 `yzr-multi-agent-context/references/layering.md` 骨架 + 仓库自身 `AGENTS.md`）；删除 0.24.0 的两处面向 Codex 的段内 HTML 注释 Read 指引（其职责由顶部指令接管——R2「段内不再单挂指引 / 一条通则兜底，不绑定具体 agent」）。SSOT（模板 + spec + canonical）去品牌 R1：`Claude Code` / `Qoder` / `Codex` 点名一律改行为化措辞（"自动展开 `@import` 的 agent" / "不展开 `@import` 的 agent" / "经薄壳加载的 agent"）；`CLAUDE.md` 薄壳保留 `Claude Code`（标题 + 薄壳声明 = R5 逃生舱）。`check_wiki_fixtures.py` 的 `agents-md-codex-read-hint`（warn）**重命名 + 改逻辑**为 `agents-md-top-read-directive`（断言首个 `## ` 前 blockquote 区含 `@` + `Read`，agent 无关）——fixtures count 不变（仍 18）。**顺带修预存漂移**：`fixtures/scripts.md.txt` 与 `canonical/scripts.md` 之前不一致（README 声称无占位符文件字节相同），现已同步。SKILL.md `metadata.wiki_spec_version` 升至 0.25.0（`fixtures_check_count` 仍 18）；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.25.0；`references/{agents-md,claude-md}-template.md` 顶部改造；`references/canonical/{memory-index,scripts}.md` + `references/fixtures/{memory-index.txt,scripts.md.txt}` 顶部加载机制注释去品牌 + 同步；`references/wiki-spec.md` §2 / §5 / §5.1 / §14.3 去品牌 + §14.8 改写 + 新增 §14.9 迁移；`references/lint-checklist.md` §二.14 去品牌（删 0.23.0 stale 内联叙事）。**破坏性**：0.24.0 wiki 升级后——`agents-md-top-read-directive` 会 warn（缺顶部指令）；LLM agent 按 §14.9 走轻量迁移（加顶部指令 blockquote + 删两处 Codex HTML 注释 + 可选去品牌 + 改 §八版本号）。`MEMORY/MEMORY.md` 与 `scripts/SCRIPTS.md` 本体不动 |
| 0.24.0 | 2026-07-11 | **回退 0.23.0：L2 索引恢复 `@MEMORY/MEMORY.md` / `@scripts/SCRIPTS.md` `@import` 收口**——`AGENTS.md` 顶部重新挂两行 `@import`（`@MEMORY/MEMORY.md` + `@scripts/SCRIPTS.md`），§一两个 `#### 跨会话记忆（索引）` / `#### Wiki-local scripts（索引）` 子段**删除**（其内容由两边 `@import` 自动展开，AGENTS.md 不再持有副本）。`scripts/SCRIPTS.md` 形态恢复 0.22.0 单段双形态（`## 索引` 段兼做 AGENTS.md 投影 + 详细契约的共享源）。**根因**：0.23.0 内联方案踩了"双写漂移 / L1 膨胀 / 真源分裂 / 三家不需要统一姿势"四个坑（详见 `yzr-multi-agent-context/SKILL.md`「为何不再用内联」段）；新事实是 Qoder 原生支持 `AGENTS.md` 内 `@MEMORY/...` import，故 Claude Code（递归）+ Qoder（原生）两家透明展开，只有 Codex 例外——AGENTS.md 项目记忆段加一段面向 Codex 的 HTML 注释显式 Read `MEMORY/MEMORY.md` 指引即可。**新增** `R2 @import 收口` checks：`agents-md-has-at-imports`（error，断言 AGENTS.md 顶部 `@MEMORY/MEMORY.md` + `@scripts/SCRIPTS.md` 两行单行形式均存在）替代 0.23.0 的 `agents-md-no-at-imports`；`agents-md-codex-read-hint`（warn，断言 `## 跨会话记忆（索引）` 段含 Codex 不展开 `@import` HTML Read 指引注释——同样移到顶层）。**删除**：`agents-md-inline-index-sections`（不再要求 h4 段）；`INLINED_INDEX_MAX` 常量 + `check_inlined_memory_index_size()` 函数 + `inlined-memory-index-bloating` warn（仅护内联方案的内联条数护栏已废——索引走 `@import` 不占 L1 词数）。fixtures count 由 20 回到 **18**（`check_wiki_fixtures.py` CHECK_REGISTRY -2）。SKILL.md `metadata.wiki_spec_version` 升至 0.24.0 + `metadata.fixtures_check_count` 由 20 降至 18；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.24.0；`references/wiki-spec.md` §5（恢复"@import 自动展开"小节）/ §5.1 / §5.2（删除"内联索引条数护栏"整段）/ §11 / §14.3 / §14.4 / §目录树 / §5 加载机制段 / §14.7（撤销 §14.8 迁移指南）全部同步回退到 0.22.0 `@import` 口径；`references/lint-checklist.md` §二.15「AGENTS.md 内联 MEMORY 索引条数阈值」整章删除 + 目录重排（related/compared 回到 §15）；`references/agents-md-template.md` §一 顶部两行 `@import` 回来 + 两个 `####` 子段删除 + 引用 `[§14.7 迁移指南]` 一并退场；`references/claude-md-template.md` 顶部声明恢复 0.22.0 形态（点明经薄壳 `CLAUDE.md → @AGENTS.md` 递归展开 `@MEMORY/MEMORY.md` / `@scripts/SCRIPTS.md`）；`references/canonical/{memory-index,scripts}.md` + `references/fixtures/{memory-index,scripts.md}.txt` 顶部注释恢复"`@MEMORY/MEMORY.md` import 会话常驻 / 正文按需 Read"。**破坏性**：0.23.0 wiki 升级后——AGENTS.md §一删除两个 h4 段 + AGENTS.md 顶部加两行 `@import` + 删除 §15 索引内联副本；`MEMORY/MEMORY.md` 与 `scripts/SCRIPTS.md` 本体**不动**（仍是真源）；迁移后 Codex 通过 HTML 注释指引 Read `MEMORY/MEMORY.md` 拿到索引，Claude Code / Qoder 透明走 `@import` 自动展开——这是把"内联精神"（不让 `@import` 失效成裸字符串）落到**机制层**（`@import` 现在三家起码 2 家真支持 + Codex 例外 Read 指引），不再用"双写 + bloat guard"的工程式补丁 |
| 0.23.0 | 2026-07-11 | **`AGENTS.md` L2 索引改内联（多 agent 真兼容）+ 内联索引条数 bloat guard**——`@MEMORY/MEMORY.md` / `@scripts/SCRIPTS.md` 两行 `@import` 从 `AGENTS.md` 顶部**删除**，其内容改为**内联**进 `AGENTS.md` 正文：§一 `#### 跨会话记忆（索引）` 段逐字内联 `MEMORY/MEMORY.md ## 索引` 段全部条目；§一 `#### Wiki-local scripts（索引）` 段逐字内联 `scripts/SCRIPTS.md ## 索引` 段 one-liner 列表。**根因**：`@import` 递归展开只 Claude Code 支持（经薄壳 `CLAUDE.md → @AGENTS.md` 展开），Codex / Qoder / Gemini CLI 不展开 `AGENTS.md` 内的 `@import`——0.22.0 之前整个 `MEMORY/` 与 `SCRIPTS.md` 对它们**不可见**，"多 agent 兼容"沦为口号。**内联后所有 agent 一视同仁**（论证详见 `yzr-multi-agent-context/SKILL.md`「L2 陷阱段」）。`scripts/SCRIPTS.md` 形态改为双段：顶部 `## 索引`（紧凑 one-liner 给 AGENTS.md 投影）+ 下方 `## 分节契约（详细）`（每脚本 `### <name>` 段，4 要素契约）。`check_wiki_fixtures.py` 新增 **2 条** check（共 **20 条**）：`agents-md-no-at-imports`（error，扫 `AGENTS.md` 残留 `@MEMORY` / `@scripts` / `@wiki/tags.md` 等 `@import` 行）+ `agents-md-inline-index-sections`（warn，迁移期兜底——扫 `#### 跨会话记忆（索引）` + `#### Wiki-local scripts（索引）` 两个 h4 段是否齐备）。`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.23.0；新增常量 `INLINED_INDEX_MAX = 50` + 新增 `check_inlined_memory_index_size()`——扫 `AGENTS.md` §一 `#### 跨会话记忆（索引）` 段 bullet 数，超过阈值报 `inlined-memory-index-bloating` warn（**仅护 AGENTS.md 投影**，`MEMORY.md` 本体可自由增长）；`severity_of()` 加 `inlined-memory-index-bloating → warn` 映射。SKILL.md `metadata.wiki_spec_version` 升至 0.23.0 + `metadata.fixtures_check_count` 由 18 升至 20；`scripts/check_wiki_fixtures.py` CHECK_REGISTRY +2。`references/wiki-spec.md` §2 / §5 / §5.1（含新增"内联索引条数护栏"段）/ §5.2 / §11 / §14.3 / §14.4 / §14.6 / §14.7(新增 §14.8 迁移指南) / §目录树 / §5 加载机制段 全部同步重写对齐"内联"口径；`references/lint-checklist.md` 新增 §二.15「AGENTS.md 内联索引条数阈值」+ §二.14 措辞同步 + 目录重排（§15 related/compared → §16）；`references/agents-md-template.md` §一 新增两个 `####` 子段 + 各加一条"内联索引条数护栏"纪律条目、`@import` 行删除；`references/claude-md-template.md` 顶部声明改写（去掉 `@MEMORY/MEMORY.md` / `@scripts/SCRIPTS.md` 提及）；`references/canonical/{memory-index,scripts}.md` + `references/fixtures/{memory-index,scripts.md}.txt` 顶部注释更新加载机制。**破坏性**：0.22.0- 老 wiki 升级后会立即报 `agents-md-no-at-imports`——LLM agent 按 §14.8 7 步迁移（删 `@import` 行 + 增两个 `####` 段 + 改 §八 版本号 + 不写 log + 不改 MEMORY.md / SCRIPTS.md 本体）。迁移后所有 agent 一视同仁看到 MEMORY 索引与 scripts 列表——这是 0.22.0 起承诺但实际未达成的"多 agent 兼容"的真正落地；老 wiki 升级后还会**新报** `inlined-memory-index-bloating` warn（若内联后条目 > 50），但属非阻断 warn，由 agent 选短条目化 / 分类摘要 / 回写 MEMORY.md 三选一 |
| 0.22.0 | 2026-07-11 | **related / compared frontmatter 路径格式约定（wiki 根相对）+ 新增 `related-broken-link` lint check（warn）**——`wiki-spec.md §9`「类型特化字段」表 `related` / `compared` 行明确路径格式约定：**wiki 根相对路径**（如 `concepts/transformer.md`），裸文件名（`transformer.md`）与文件相对路径（`../concepts/transformer.md`）均不再被接受。新增 `§9 路径格式约定`段说明两层约定区分——frontmatter 路径字段（机器消费为主，wiki 根相对、跨子目录不丢信息）vs 正文 markdown 链接（人读为主，文件相对、in-context 局部）。`scripts/lint_wiki.py` 新增 `check_related_links()` 扫 `related` / `compared` 字段每条元素，按 wiki 根相对解析，`is_file()` 判定；不命中 → 报 `related-broken-link`（warn）——与正文 `broken-link`（error）严重性区分开（frontmatter 是机器消费、非人直接阅读内容）。`references/lint-checklist.md` 新增 §二.15 段；`severity_of()` 加 `related-broken-link → warn` 映射。`references/page-templates.md` concept / comparison 模板示例同步更新为 wiki 根相对路径。**非破坏性**：`related` / `compared` 字段为可选（spec §9「必填」列均为否），缺字段的 wiki 升级零影响；已有的裸文件名 / 文件相对路径 wiki 升级后会一次性报出所有 `related-broken-link` warn，由用户批量改写为 wiki 根相对形式。`contradictions` 字段按 spec §9 既有约定**保留**文件相对解析（与 `related` / `compared` 两层区分是有意保留），不在本检查范围。SKILL.md `metadata.wiki_spec_version` + `scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.22.0 |
| 0.21.0 | 2026-07-11 | **raw/external/ sources 允许指向目录（bug fix）**——`scripts/lint_wiki.py` `check_sources_field` external 分支（第 657 行）由 `sp.is_file()` 改为 `sp.exists()`：external repo 本身是 git 仓（即目录），`sources: [raw/external/<symlink>]` 语义合法；原 `is_file()` 对目录返回 `False` 导致误报 `sources-missing`。finding 消息由「文件不可访问」改为「路径不可访问」。普通 raw 路径（非 `raw/external/`）的 sources 仍要求指向**文件**——raw 子树语义是「已 ingest 的文档」，目录型 raw 来源暂无用例。spec §13.3「LLM agent 责任」补 `sources:` 元素类型澄清（文件或目录皆可，仅 external 分支）；`references/lint-checklist.md` §二.3 step 4 措辞同步。**非破坏性**：合规 0.20.0 wiki 升级 = bump AGENTS.md §八版本号即可，无内容改动；之前因 external sources 指向整仓而误报 `sources-missing` 的页面升级后自动消失。SKILL.md `metadata.wiki_spec_version` + `scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.21.0 |
| 0.20.0 | 2026-07-08 | **fixtures 字段级骨架比对 + 常规 lint 版本检查 + 升级末尾清理**——`check_wiki_fixtures.py` 从 11 条扩到 **18 条** check：新增 7 条骨架字段比对（`gitignore-init-rules-complete` / `index-md-frontmatter-complete` / `index-md-skeleton` / `log-md-frontmatter-complete` / `memory-index-skeleton` / `scripts-md-skeleton` / `tags-md-skeleton`），读 `references/canonical/` + `references/fixtures/gitignore.txt` 作 **SSOT** 做字段级骨架比对（改 fixtures → check 自动跟随）：纯骨架件（.gitignore / tags.md / SCRIPTS.md / MEMORY.md）全字段骨架比对，成长件（index.md / log.md）只比结构必填（frontmatter 键 + H1 + 说明块），**不动成长内容**。`build_migration_plan()` 新增 `fixtures-fix-skeleton` 通用 action 类型（匹配 `*-skeleton` / `*-frontmatter-complete` / `*-init-rules-complete` cid，`to_action` 引用 expected 缺失项）。常规 lint 新增 `check_spec_version()`——每次 lint 查 AGENTS.md §八版本与 `CURRENT_WIKI_SPEC` 一致性，落后 / 领先给 warn 提示走 `--check-version` 升级（finding `wiki-spec-version-stale` / `-ahead` / `-unparsed`）。升级流程末尾新增「清理临时文件」步（删 `.migration-plan.json` + `*.bak`）。`fixtures/gitignore.txt` 加 `.migration-plan.json`（升级中间产物，用完即删 + 不进 git）。**纯检测覆盖增量，无文件格式变更**：合规的 0.19.0 wiki 升级 = bump AGENTS.md §八版本号即可，无内容改动；缺骨架字段的老 wiki 才首次收到 `fixtures-fix-skeleton` action。SKILL.md `metadata.wiki_spec_version` + `scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.20.0；`SKILL.md` §0.18.0+ 段 + §5 Migrate + `references/migrate-workflow.md`（新增「fixtures 字段更新清单」节 + step 8 清理）+ `references/lint-checklist.md`（§二前置版本检查 + §八边界）+ `scripts/check_wiki_fixtures.py` docstring 同步 |
| 0.19.0 | 2026-07-08 | **MEMORY frontmatter 解耦 + raw/external/ sources 例外**——`scripts/lint_wiki.py` `check_frontmatter` 拆分为两段：wiki 5 类内容页保留 5 必填（`title`/`type`/`created`/`updated`/`tags`），**MEMORY/*.md 改为仅 `title` 必填**（其余 5 字段全 optional——spec §5「agent 私有」定位 + 与短条目「1 行索引行」形态对齐）。`VALID_TYPES` 扩展 `memory` / `memory-entry` 两类（spec §5.2 + §9）；`check_reviewed_signals` 与 `check_tag_taxonomy` 的 `target_pages` 不再含 MEMORY 桶（MEMORY 是 agent 私有，不走 wiki 用户面 reviewed 校验 + 不共享 tag taxonomy）。`check_sources_field` 加 `raw/external/<symlink>/...` 例外分支（spec §13.3）：`.resolve()` 后落 wiki 根外是 symlink 预期行为，改为校验 anchor + symlink 存在 + 文件可访问；新增 finding `sources-external-anchor-missing` / `sources-external-symlink-missing` / `sources-malformed`。**破坏性**：`type: memory` / `type: memory-entry` 在 MEMORY/*.md 上从「非法」变「合法」（无需迁）；5 必填 MEMORY 老页面继续合法（不强制减字段）；wiki 5 类内容页规则不变。SKILL.md `metadata.wiki_spec_version` + `scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.19.0；`references/lint-checklist.md` §5.1 / §二.2 / §二.3 / §二.11 / §二.13 / §九.1 + `references/page-templates.md` §一 + `references/wiki-spec.md` §5.2 全部同步重写对齐新口径 |
| 0.18.0 | 2026-07-07 | **fixtures 一致性检查能力增量**——`--check-version` 自动调一次 `scripts/check_wiki_fixtures.py`，扫 wiki 约定文件（AGENTS.md §八版本行 / .gitignore / wiki/index.md / wiki/log.md / wiki/tags.md / MEMORY/MEMORY.md / scripts/SCRIPTS.md / raw/external/.symlink-anchor.toml）是否满足当前 wiki spec 的字节合规，并入 report["fixtures_check"]；`build_migration_plan()` 新增 `fixtures_actions[]` 数组（与 legacy `actions[]` 平行；type 前缀 `fixtures-fix-*`，含 `expected`/`actual`），落 `.migration-plan.json` 供 agent 走 Edit/Write 修约定文件。**11 条** check：agents-version-is-current / gitignore-external-track-toml / symlink-anchor-toml-schema / symlink-anchor-toml-symlink-matches / symlink-anchor-flat-not-legacy / index-md-categories-stable / memory-index-no-frontmatter / memory-entries-indexed / log-md-format-strict / scripts-md-no-frontmatter / tags-md-no-frontmatter。`fixtures-fix-anchor-merge / -anchor-schema / -anchor-symlink-matches` 三条按 `lint-checklist.md` §五.3 五步迁移（不是单 Edit）——其余可单 Edit 落。**语义合并规则**落到 `references/lint-checklist.md` §五（frontmatter 合并 / index 重复条目 / 0.16.0 → 0.17.0 anchor 多 entry 归并 / MEMORY supersede 等），由 LLM 按该规则人工合并——scripts/ 不替代语义判断（脚本只产 plan）。SKILL.md `metadata.wiki_spec_version` 升至 0.18.0；`SKILL.md` §1 交付物 + §5 Migrate + 「输入 / 输出」 + 「四个核心操作」段加 fixtures-check 描述；`scripts/lint_wiki.py` `CURRENT_WIKI_SPEC` 升至 0.18.0 + `_run_fixtures_check()` 新增（subprocess 调 check_wiki_fixtures.py + 解析 JSON）+ `build_migration_plan()` 新增 `fixtures_actions` 落盘 + `_print_fixtures_check()` 新增 |
| 0.17.0 | 2026-07-07 | **§13 raw/external/ 扁平化 + anchor 改 TOML**：anchor 从「每仓一份 `<source-name>/.symlink-anchor.json`（JSON object）」改为「单文件 `external/.symlink-anchor.toml`（TOML array-of-tables，支持注释）」——所有外部仓的 symlink 直接放在 `raw/external/` 下、共享一份 anchor。`[[entry]]` 数组每元素含 `symlink` / `target` / `captured_at` / `kind` 必填 + git 扩展字段（git 仓时必填）+ `tag` / `notes` 可选；顶层 `schema_version = 1` 可选。**破坏性变更**：0.16.0- 老 wiki 跑 `lint_wiki.py` 会报 `external-anchor-missing`（旧 `<source-name>/.symlink-anchor.json` 不再被识别）——LLM agent 按 §13.6 迁移指南手工 Edit/Write 升级（`--check-version --apply` 不为此自动迁移，结构差异太大）。SKILL.md `metadata.wiki_spec_version` 升至 0.17.0；`scripts/lint_wiki.py` `check_external_symlinks` + `_parse_anchor` + `_check_git_anchor` 全部重写——`tomllib`（py3.11+）解析、entry 级 git 字段校验、新增 finding `external-anchor-orphan`（warn：symlink 存在但 anchor 无 entry）/ `external-symlink-missing`（error：anchor 有 entry 但 symlink 不在 `external/` 顶层）/ `external-source-name-invalid`（error：symlink 命名不合 `^[a-z0-9][a-z0-9-]*$`）。`agents-md-template.md` §一 / `external-repo-rebuild.md` / `lint-checklist.md` / `SKILL.md` §1.bulk + 核心原则 + 边界 + 反模式 / `references/fixtures/gitignore.txt`（`**/.symlink-anchor.json` → `.symlink-anchor.toml`）同步对齐扁平形态。`subpath` 字段退役——直接创建多 symlink 表达 |
| 0.16.0 | 2026-07-07 | **红线贯彻：CLI 撤掉全部 git 操作**——§7 重写。CLI init 仅落盘目录结构 + touch .gitkeep + 打印 hint，**完全不碰 git**（不 `git init` / `git symbolic-ref` / `git config` / `git add` / `git commit`）。**根因**：0.16.0 前 CLI 在 `--git` opt-in 时自动跑 `git init` + `git symbolic-ref HEAD refs/heads/main` + `git config user.email` 占位 + `git add .` + `git commit`，违反"所有 git 操作由用户触发"红线。CLI 主动 git init 还会让 raw/ 整体 0 tracked 等问题被"假提交"掩盖（init 时仓是空的，commit 后 raw/articles/ + raw/assets/ 仍是空目录、靠 0.15.0 的 `.gitkeep` 占位才进 git，但前提是 CLI 真做了 git add）。CLI 退回纯落盘后，**用户**看到 wiki 落盘后**自己决定**是否 init + add + commit——所有 git 操作都可追溯到用户意图。**破坏性**：CLI 0.16.0+ 升级后，老用户（CLI 0.16.0 之前 init 过 git 的 wiki）**不受影响**——git 仓已存在、history 已写，新 CLI 不再主动 add/commit 不会破坏 history；新用户（首次 init）需手动 `cd <wiki-root> && git init && git add . && git commit -m "..."`。**包含 0.15.0 设计的 raw/.gitkeep 占位**——CLI 仍 touch `raw/articles/.gitkeep` + `raw/assets/.gitkeep`，但**不**自动 `git add`；用户首次 `git add .` 时由 git 自然纳入（前提是用户 init git，否则纯目录树）。SKILL.md `metadata.wiki_spec_version` 升至 0.16.0；`lint-checklist.md` §二.1 raw-modified 措辞更新（前提改为"wiki 仓在 git 内 + raw/ 有 tracked 文件"——CLI 不再保证 git init，"未启用 git"是更常见的状态）；`agents-md-template.md` §一 加"git 由用户触发"纪律 |
| 0.14.0 | 2026-07-07 | §13.2 `target` 字段类型放宽为「绝对路径 **或** `~/...` home-relative 形式（推荐后者）」——同 home 布局的多机可共享同一 anchor 不需重写；`scripts/lint_wiki.py` 在 §13.5 校验前已 `Path(target).expanduser()` 统一展开，**不**需新增 finding（绝对路径展开后不变，`~/...` 展开为 `$HOME/...`）。SKILL.md `metadata.wiki_spec_version` 升至 0.14.0；`SKILL.md` §1.bulk / `agents-md-template.md` / `external-repo-rebuild.md` / `lint-checklist.md` §二.3 措辞同步对齐"允许 `~/...`"。**老 wiki 兼容**：0.13.0 及更早的 anchor 写的是绝对路径，`expanduser` 展开后等价不变，lint 行为不变；新接入/重写 anchor 时推荐改用 `~/...` 形式（用户主动改） |

> **更早版本**（0.13.0 → 0.1.0）摘要：`sources-absolute-path` finding（0.13.0）/ `raw/external/`
> schema + git 锚定（0.12.0）/ AGENTS.md SSOT + CLAUDE.md 薄壳拆分（0.11.0）/ MEMORY/ 平级
> 目录（0.10.0）/ `scripts/` + `SCRIPTS.md`（0.9.0）/ `wiki/tags.md` tag 白名单（0.8.0）/
> `reviewed` / `reviewed_at` 二态替代 `confidence`（0.7.0）/ MEMORY 索引改名 MEMORY.md
> （0.6.0）/ `contested` / `contradictions` 字段（0.5.0）/ `raw/external/` 首次引入（0.4.0）/
> git 初始化 opt-in（0.3.0）/ MEMORY 目录首次引入（0.2.0）/ 初始版本（0.1.0）。详细 changelog
> 见 git log / 老 wiki 的 spec 历史 tag。
