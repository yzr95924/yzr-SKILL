---
name: my-skill-name
description: |
  当用户<场景>时使用本 skill，<干什么 + 关键能力>。
  触发：<用户原话 / 场景描述>
  不适用：<负例>
metadata:
  author: 你的名字
  modify time: YYYY-MM-DD
---

# my-skill-name

一句话总述：这个 skill 让 agent 能干什么、产出什么（description 决定何时调，这句给已加载的 agent 定调）

## 输入与输出

| 方向 | 内容 |
| --- | --- |
| 输入 | <用户给什么 / agent 需要什么前置> |
| 输出 | <skill 交付什么：文件 / 结论 / 行为变化> |

## 执行原则

<贯穿全程的判断基线：不单独属于某一步的规则、红线、以及"为什么"。视角 / 立场
一两句话能讲清 = 写成一条列表项（立场 + 为什么）；核心价值是判断且立场需要成段展开
（review / 评审 / 审计类）= 在本节后立一个领域节（如"评审立场" / "设计决策"），机械型 skill 不加
`reference` 型（纯参考资料：只聚合信息、不改变行为决策）可省略本节与工作流节>

## 工作流

<按顺序的操作步骤。`meta` 型（元 / 多入口）在第一个规范节之前加一个 `## 入口` 节，
`ref/` 按入口 / 领域一文件，正文只留工作流 + 触发选择>

## 参考样例

<1–2 个真实用户输入 → 本 skill 介入路径的映射。与 description 的"触发"槽分工：
description 放触发短句（决定何时调），此处放介入路径演示（决定怎么干）。可省略>

<文末不设"参考文件"索引节："何时去读"与路由表都写在用它的工作流步骤行内>
